# Optional. Leave unset when using the local Vite proxy or the Vercel rewrite.
# Set this to the backend URL (including /api) only when calling it directly.
VITE_API_URL=https://hr-dashboard-ynkt.onrender.com/api
